namespace Notchle.Core.App;

// STUB: agent W3 replaces this. Platform-neutral port of Sources/Notchle/AppCoordinator.swift:
// runs GameEffects against ITrackSource / IPlayer / ProgressStore, serialises playback
// (a new operation cancels the running one and awaits it before starting), and publishes
// state through a callback the WPF layer marshals onto its dispatcher.
public sealed class AppCoordinator
{
}
